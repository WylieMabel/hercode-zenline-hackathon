# Optional: for deployed Streamlit apps only. Prefer the in-app password field for local use.
# Copy to .streamlit/secrets.toml (gitignored) — never commit real keys.
#
# CLAUDE_API_KEY = "sk-ant-..."
