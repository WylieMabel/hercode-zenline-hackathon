# Optional — not required for bundled demo mode.
# Copy to .env locally if you prefer env vars over the Streamlit sidebar.

# Claude (recommendations + config enrichment)
# CLAUDE_API_KEY=sk-ant-...

# YouTube (only for refreshing data/bundled/*.csv via scripts/snapshot_bundled_signals.py --live)
# YOUTUBE_API_KEY=
